# Open Issues

Task: `POST_HUMAN_DRAFTING_RULE_UPDATE_REGRESSION_TEST`

## Remaining Issues Confirmed

1. Reset/boot cluster topology is still electrically wrong in the saved
   schematic.
2. Local dark return cluster near `D1/D2/C3/C4/C6/SW1` is not actually on
   `GND`.
3. Text overlap still fails on the `LEDS` heading and several dense values.
4. Human visual proof is still incomplete.
5. PCB update remains blocked by footprint/package and separate project gates.

## Status

These are confirmed active blockers, not newly invented issues.
