# ESP32_CSI_WIFI_NODE PCB Prelayout Variants Open Items

- `J1` exact barrel-jack orientation proof remains `NEEDS_HUMAN_REVIEW`.
- The project footprint/package gate is still not `PASS`, primarily because `U2` and `U3` exact package proof is unresolved.
- The live board still reports `13` unconnected items and `3` detectable unrouted nets.
- The fresh prelayout packet produced `0` passing variants, so no real placement application is allowed.
