# Hallucination Risk Log

- Avoided inferring secret safety from absence of `.env` alone; used targeted
  scans and reported false-positive context explicitly.
- Avoided claiming the active ESP32 project is healthy; used live-state and
  final-review evidence showing it is still blocked.
- Avoided claiming the dirty schematic is safe to commit; left it as a human
  scope decision.

