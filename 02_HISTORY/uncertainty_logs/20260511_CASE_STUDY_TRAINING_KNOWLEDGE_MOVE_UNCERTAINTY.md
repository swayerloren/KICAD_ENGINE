# Uncertainty Log

- The confidence labels used in this phase are migration-policy labels, not a
  new canonical source-scoring taxonomy for the whole repo.
- Some official-source pages were present inside the mixed `good_board` and
  `university_training` folders, but this phase intentionally kept the raw
  captures quarantined because the folders themselves were mixed and not safe to
  promote wholesale.
- The top-level `knowledge_scrape/` folder remains non-empty because other
  categories are still pending future migration prompts.

