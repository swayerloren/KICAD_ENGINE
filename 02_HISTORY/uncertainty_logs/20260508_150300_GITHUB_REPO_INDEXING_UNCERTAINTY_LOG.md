# Uncertainty Log

Date: `2026-05-08`

- The new indexes improve navigation substantially, but they do not guarantee every legacy subtree is fully normalized.
- Some historical documents still reference the older non-GitHub checkout path; I corrected the main entry points but did not rewrite every historical file.
- The active-project blocker summary is based on the latest committed project-memory and report files, not a fresh KiCad DRC rerun in this documentation-only task.
