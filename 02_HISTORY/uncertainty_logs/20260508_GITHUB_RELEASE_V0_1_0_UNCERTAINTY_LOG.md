# Uncertainty Log

- The GitHub release API returns `zipball_url` as an API endpoint; for human download guidance the stable browser-friendly source ZIP path is inferred as `https://github.com/swayerloren/KICAD_ENGINE/archive/refs/tags/v0.1.0.zip`.
- No binary release assets were uploaded; the release currently relies on GitHub source archives.
