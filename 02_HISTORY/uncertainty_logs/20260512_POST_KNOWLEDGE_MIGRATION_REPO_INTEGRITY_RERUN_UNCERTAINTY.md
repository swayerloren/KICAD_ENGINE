# Uncertainty Log

- The broad secret scan remains intentionally noisy because docs and historical
  records contain placeholder token names and masked examples.
- The rerun classification assumes later staging will stay explicit and will not
  silently include the dirty schematic file.
- Full-repo historical/generated Markdown may still contain stale references,
  but the targeted active startup/routing link audit passed with `0` missing
  links and `0` bad active-route references.

