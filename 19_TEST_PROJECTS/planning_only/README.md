# Planning-Only Test Projects

Status: `SAFE_PLANNING_ONLY`

This folder holds non-KiCad planning fixtures. These are safe for testing AI planning workflows because they do not contain `.kicad_pro`, `.kicad_sch`, `.kicad_pcb`, library files, or manufacturing outputs.

