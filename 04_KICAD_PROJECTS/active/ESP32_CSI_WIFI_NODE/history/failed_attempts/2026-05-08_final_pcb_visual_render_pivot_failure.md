# Final PCB Visual Render Pivot Failure

Status: `RECOVERED`

Direct `kicad-cli pcb render` camera-pivot close-ups were not reliable enough for the final LJ packet. The accepted recovery was to keep the fresh full-board renders and generate deterministic crops from those images instead.
