# Hallucination Risk Log

Session: `PCB_BATCH_04_CONTROL_NET_ROUTING`

Date: `2026-05-08`

- Low risk on the applied `/U0RXD` claim because the route, hash delta, DRC result, and visuals were all verified directly.
- Medium risk on the exact root cause summary for every `/BOOT0` and `/ESP_EN` failed candidate because only representative failed windows were inspected in detail, not every generated trial.
- No unsupported claim is made that `/BOOT0` or `/ESP_EN` are impossible; only that no `0`-violation candidate was found in this pass.
